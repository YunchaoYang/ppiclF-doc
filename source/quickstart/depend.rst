.. _depend:

------------
Dependencies
------------
A valid Fortran and C compiler is required. 

Additionally, a vaild Message Passing Interface (MPI) standard is required. On ubuntu, this can be installed using

.. code-block:: bash
   
   sudo apt-get install -y libmpich-dev mpich
